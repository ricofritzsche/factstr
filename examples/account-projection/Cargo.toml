[package]
name = "factstore-example-account-projection"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
factstore = { path = "../../factstore" }
factstore-memory = { path = "../../factstore-memory" }
serde_json = "1"
